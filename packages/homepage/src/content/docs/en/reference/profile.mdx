---
title: "devsync profile"
description: "Manage sync profiles."
---

Manage active and assigned sync profiles.

Inspect configured profiles or update which profile devsync should use by default.

## Subcommands

### `devsync profile list`

Show configured and active profiles.

```bash
devsync profile list
```

### `devsync profile use [name]`

Set or clear the active profile. When called without a name, the active profile is cleared.

```bash
devsync profile use [name]
```

## Examples

List all configured profiles:

```bash
devsync profile list
```

Activate the work profile:

```bash
devsync profile use work
```

Clear the active profile:

```bash
devsync profile use
```
