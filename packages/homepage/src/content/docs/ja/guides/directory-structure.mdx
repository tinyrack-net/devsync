---
title: ディレクトリ構造
description: devsyncが設定ファイルと同期データを保存する場所と構成について。
---

import { Aside, FileTree } from "@astrojs/starlight/components";

devsyncが作成するものはすべて`~/.config/devsync/`ディレクトリにまとまっています。`devsync init`を実行すると、この構造が自動的に作成されます。

---

## 全体像

<FileTree>
- ~/.config/devsync/
  - settings.jsonc
  - keys.txt
  - repository/
    - manifest.jsonc
    - default/
      - .gitconfig
      - .zshrc
      - .config/
        - nvim/
          - init.lua
    - work/
      - .config/
        - work-tool/
          - config.json
      - .ssh/
        - config.devsync.secret
</FileTree>

---

## 各ファイルの役割

### `repository/` — syncディレクトリ

同期されたファイルを保持するGitリポジトリです。`devsync push`を実行するとファイルがここにコピーされ、`devsync pull`を実行するとここからファイルが復元されます。GitHubなどのリモートにpushすることで、マシン間で同期できます。

### `repository/manifest.jsonc` — 設定ファイル

設定ファイルはすべての真実の源です。トラッキング中のすべてのファイルとディレクトリが、同期モード、プロファイル、パーミッションなどのオプションとともにリストされています。`devsync track`や`devsync untrack`コマンドがこのファイルを更新します。

### `repository/default/` — defaultプロファイルのアーティファクト

特定のプロファイルに割り当てられていないファイルはここに保存されます。ほとんどのdotfileはここに配置されます。

### `repository/<profile>/` — 名前付きプロファイルのアーティファクト

[プロファイル](/ja/guides/profiles)を使用する場合、各プロファイルに専用のサブディレクトリが作られます。例えば、`--profile work`でトラッキングされたエントリは`repository/work/`に保存されます。

### `keys.txt` — ageのidentityファイル

[シークレットファイル](/ja/guides/syncing-secrets)の暗号化と復号に使うageプライベートキーです。`devsync init`の実行時に作成され、`AGE-SECRET-KEY-`で始まります。

<Aside type="caution">
`keys.txt`は安全な場所にバックアップしてください — パスワードマネージャーがおすすめです。紛失すると、シークレットファイルを復号できなくなります。
</Aside>

### `settings.jsonc` — ユーザー設定

アクティブなプロファイルやその他のローカル設定が保存されます。通常は直接編集する必要はなく、`devsync profile use work`のようなコマンドが自動的に更新します。

---

## `manifest.jsonc`の内容

トラッキングエントリが数件ある場合の例です：

```json
{
  "version": 7,
  "age": {
    "recipients": ["age1ql3z7hjy54pw3hyww5ayyfg7zqgvc7w3j2elw8zmrj2kg5sfn9aqmcac8p"]
  },
  "entries": [
    {
      "kind": "file",
      "localPath": { "default": "~/.gitconfig" }
    },
    {
      "kind": "directory",
      "localPath": { "default": "~/.config/nvim" }
    },
    {
      "kind": "file",
      "localPath": { "default": "~/.ssh/config" },
      "mode": { "default": "secret" },
      "profiles": ["work"]
    }
  ]
}
```

### 設定ファイルの構造

- **`version`** — スキーマバージョン（現在は`7`）。devsyncが自動的に管理します。
- **`age`** — 暗号化設定。`recipients`配列にはシークレットファイルの暗号化に使用する公開鍵がリストされています。
- **`entries`** — トラッキング中のファイルとディレクトリの一覧。各エントリには以下のフィールドがあります：
  - **`kind`** — `"file"`または`"directory"`
  - **`localPath`** — マシン上のファイルの場所（[プラットフォーム別パス](/ja/guides/platform-specific-paths)対応）
  - **`repoPath`** — syncディレクトリ内のカスタムパス（省略可。デフォルトでは`localPath`から自動生成）
  - **`mode`** — [同期モード](/ja/guides/sync-modes)：`"normal"`、`"secret"`、`"ignore"`（省略可。デフォルトは`"normal"`）
  - **`profiles`** — このエントリが属する[プロファイル](/ja/guides/profiles)（省略可。省略時はグローバル）
  - **`permission`** — `"0600"`のような8進数のファイルパーミッション（省略可）

<Aside type="note">
`manifest.jsonc`を手動で編集する必要はほとんどありません。`devsync track`や`devsync untrack`コマンドが管理してくれます。
</Aside>
